// Nickname system removed — displays are identified by GDI short name (DISPLAY1)
// and audio devices by FriendlyName pattern + type, both stored directly in profiles.
namespace DisplayAudioOrchestrator.Orchestrator { }
